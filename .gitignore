# Go binaries
*.exe
*.test
*.out
server
notes-server

# Go vendor (not used)
vendor/

# Environment / secrets
.env
.env.*
!.env.example

# SQLite database files (data is external)
*.db
*.db-shm
*.db-wal

# OS files
.DS_Store
Thumbs.db
*.swp
*.tmp

# IDE
.vscode/
.idea/

# Claude Code local settings (personal permissions, not project config)
.claude/settings.local.json

# Local audit reports — never commit to remote
audit/
